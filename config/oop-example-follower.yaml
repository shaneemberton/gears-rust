server:
  home_dir: "~/.cf-gears"

# OoP gears receive this config via --config CLI arg or MODULE_CONFIG_PATH env var from master host.
# Directory endpoint is read from the shared config file.

gears:
  grpc-hub:
    config:
      # Ephemeral, registers itself in Directory
      listen_addr: "127.0.0.1:0"

  calculator:
    # Note: In OoP config, runtime.execution is not needed - it's configured in master config.
    config: { }

logging:
  default:
    console_level: debug